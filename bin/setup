#!/usr/bin/env bash

set -euo pipefail

echo "Installing gem dependencies..."
bundle install

echo "Configuring git hooks..."
git config core.hooksPath .githooks
chmod +x .githooks/pre-push

echo "Setup complete. Hooks will run RSpec before each push."
